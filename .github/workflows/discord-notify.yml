name: Discord Notifications

on:
  push:
  pull_request:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      # Récupération du repo
      - name: Checkout
        uses: actions/checkout@v4

      ##############################################################
      # 1. Calcule : commits, type, version, timestamp, etc.
      ##############################################################
      - name: Prepare Data
        id: prep
        run: |
          sudo apt-get update
          sudo apt-get install jq -y

          REPO="${{ github.repository }}"
          BRANCH="${{ github.ref_name }}"
          ROLE_ID="123456789012345678" # Mets ton rôle

          TS=$(date +%s)
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          VERSION="v-$SHORT_SHA"

          # type du commit basé sur le premier commit du push
          COMMIT_MSG=$(echo '${{ toJson(github.event.commits) }}' | jq -r '.[0].message')
          COMMIT_TYPE=$(echo "$COMMIT_MSG" | grep -Eo '^(feat|fix|docs|style|refactor|perf|test|chore)' || echo "update")

          case "$COMMIT_TYPE" in
            feat) COLOR=3066993 ;;
            fix) COLOR=15158332 ;;
            docs) COLOR=10181046 ;;
            *) COLOR=3447003 ;;
          esac

          # Nombre total de commits du repo
          TOTAL_COMMITS=$(git rev-list --count HEAD)

          # Liste propre de TOUS les commits du push
          COMMITS=$(echo '${{ toJson(github.event.commits) }}' | jq -r '.[] | "• **\(.id[0:7])** — \(.message)"' | sed ':a;N;$!ba;s/\n/\\n/g')

          echo "repo=$REPO" >> $GITHUB_OUTPUT
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT
          echo "color=$COLOR" >> $GITHUB_OUTPUT
          echo "commits=$COMMITS" >> $GITHUB_OUTPUT
          echo "total=$TOTAL_COMMITS" >> $GITHUB_OUTPUT
          echo "type=$COMMIT_TYPE" >> $GITHUB_OUTPUT
          echo "short=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "timestamp=$TS" >> $GITHUB_OUTPUT
          echo "role=$ROLE_ID" >> $GITHUB_OUTPUT

      ##############################################################
      # 12. Génération automatique du CHANGELOG.md
      ##############################################################
      - name: Generate Changelog
        run: |
          echo "## Version ${{ steps.prep.outputs.version }}" > CHANGELOG.md
          echo "Date : <t:${{ steps.prep.outputs.timestamp }}:f>" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "### Commits inclus :" >> CHANGELOG.md
          echo -e "${{ steps.prep.outputs.commits }}" >> CHANGELOG.md

      ##############################################################
      # 9/14. Message spécial si branche = main
      ##############################################################
      - name: Custom Message for Main
        if: ${{ steps.prep.outputs.branch == 'main' }}
        run: echo "🚀 Déploiement en cours sur la branche *main* !"

      ##############################################################
      # ENVOI DISCORD (Push + PR)
      ##############################################################
      - name: Send Discord Message
        run: |
          # Construction du contenu propre
          TITLE="📌 Nouvel événement GitHub sur ${{ steps.prep.outputs.repo }}"
          DESCRIPTION="**Type :** ${{ steps.prep.outputs.type }}  
          **Version :** ${{ steps.prep.outputs.version }}  
          **Total commits :** ${{ steps.prep.outputs.total }}  
          
          **Commits inclus :**
          ${{ steps.prep.outputs.commits }}"

          # Embed Discord
          cat <<EOF > payload.json
          {
            "content": "<@&${{ steps.prep.outputs.role }}>",
            "embeds": [{
              "title": "$TITLE",
              "description": "$DESCRIPTION",
              "color": ${{ steps.prep.outputs.color }},
              "fields": [
                { "name": "Branche", "value": "${{ steps.prep.outputs.branch }}", "inline": true },
                { "name": "SHA", "value": "${{ steps.prep.outputs.short }}", "inline": true }
              ],
              "footer": { "text": "GitHub Actions • Event" },
              "timestamp": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
            }]
          }
          EOF

          curl -H "Content-Type: application/json" \
               -X POST \
               -d @payload.json \
               ${{ secrets.DISCORD_WEBHOOK }}
